# Task plan

## 步骤与验证

1. 给两个占位按钮加 disabled 与提示 → 验证：渲染测试断言不可点击且提示可见。
2. 在现行 spec 记录占位功能待定义、TokenStats 独立持久化 → 验证：不宣称已实现清除/重置或主配置同步。
3. 运行 renderer 定向测试和 `pnpm test` → 验证：设置页生命周期不回归。

## 风险与回退

- 风险：disabled 样式与设计稿偏差；文档把未来方案写成既定需求。
- 回退：恢复按钮样式修改，保留准确的未实现说明。

## Finalization 时更新的 blueprint

- 无。
