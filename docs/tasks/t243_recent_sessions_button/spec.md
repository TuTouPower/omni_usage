# Task spec

契约区执行期原则上不再改动；确需调整须经用户确认（渲染 review prompt 时脚本会附契约区相对 diff_anchor 的 drift diff 供 reviewer 核对）。上下文区执行期可补。

## 背景

最近会话弹窗的快捷选择区目前只有「最近 2 个」「最近 4 个」「最近 8 个」三档，用户需要「最近 6 个」档位。

## 契约区

reviewer 判 AC 时只看本区。

### 范围

- 最近会话弹窗（`RecentSessionsModal`）快捷选择区增加「最近 6 个」按钮。

### 非范围

- 不改变弹窗的多选上限（仍为 8）。
- 不改变弹窗列表数据来源与排序。
- 不调整其他档位的既有行为。

### 验收标准

只写用户或调用方可观察行为，每条可独立验证。普通版本号、底层库和目录结构不作为验收标准；需要长期约束后续工作的技术选择写入 `docs/blueprint/decisions.md`。

需真实部署或人工环境才能验证的条目加 `[deploy]` 前缀，标明 agent 无法自证。

- [ ] AC1：最近会话弹窗快捷选择区依次显示「最近 2 个」「最近 4 个」「最近 6 个」「最近 8 个」四个按钮。
- [ ] AC2：点击「最近 6 个」后，列表中按日期倒序的前 6 个会话被选中（选择顺序角标 1-6）；会话总数不足 6 个时选中全部。

### 可测试性声明

逐条说明哪些 AC 不可自动测试及原因；全部可测则写「全部 AC 可自动测试」。

全部 AC 可自动测试。

## 上下文区

reviewer 判测试覆盖时核对本区；实施期可补。

### 有意不测

已判定不写测试的分支与原因。reviewer 不得据此出 blocking finding。无则写「无」。

无

### 测试策略

mock 边界、fixture 来源、断言目标。无特殊约定写「按项目默认」。

- 组件级测试：mock `window.usageboard.tokenStats.getSessions` 返回固定会话列表，渲染弹窗后断言快捷按钮集合与点击「最近 6 个」后的选中结果。

### 未知契约清单

尚未核实的外部 endpoint、API 形态、数据结构、第三方行为须分类标记；核实后删除标记，改为结论并注明验证方式。无则写「无」。

`UNVERIFIED-BLOCKING`：只有用户或外部环境能核实；核实前 `start` 失败。

`UNVERIFIED-SPIKE`：agent 可在执行期 Step 1 实验核实；未核实前不得进入实现。

裸 `UNVERIFIED` 属歧义格式，门禁失败。

无

### 风险与回退

- 风险：极低，纯 UI 增量。
- 回退：还原按钮数组改动即可。

### 依赖与约束

- 无。

### Finalization 时更新的 blueprint

- 无
